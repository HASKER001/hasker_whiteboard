#!/data/data/com.termux/files/usr/bin/bash

echo "🚀 Начало деплоя..."

# Добавляем все изменения
git add .

# Получаем сообщение коммита от пользователя
echo "📝 Введите сообщение коммита:"
read commit_message

# Если сообщение пустое, используем стандартное
if [ -z "$commit_message" ]; then
    commit_message="Обновление $(date '+%Y-%m-%d %H:%M:%S')"
fi

# Создаем коммит
git commit -m "$commit_message"

# Пушим на GitHub
echo "📤 Отправка на GitHub..."
git push origin main

# Проверяем результат
if [ $? -eq 0 ]; then
    echo "✅ Деплой успешно завершен!"
    echo "📁 Репозиторий: https://github.com/HASKER001/hasker_whiteboard"
else
    echo "❌ Ошибка при деплое!"
fi
